PORT=3001
NODE_ENV=development

# Email configuration (pour l'envoi à l'administrateur)
# Configuration Hostinger
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=contact@arezkidjerbi.fr
SMTP_PASS=votre-mot-de-passe-email
ADMIN_EMAIL=contact@arezkidjerbi.fr

# Webhook (optionnel, alternative à l'email)
WEBHOOK_URL=

# Calendly link
CALENDLY_URL=https://calendly.com/djerbi/audit-gratuite-pour-evaluer-vos-besoins

# Rate limiting (pour lead magnet public)
# RATE_LIMIT_MAX=5 (5 analyses par IP par jour par défaut, 0 pour désactiver)
# RATE_LIMIT_WINDOW=86400000 (fenêtre en ms, 86400000 = 24 heures = 1 jour)

# APIs gratuites pour améliorer le scoring (optionnel)
# WHOISXMLAPI_KEY= (gratuit, 500 requêtes/mois) - https://whoisxmlapi.com/
# SERPAPI_KEY= (recommandé, plus fiable que Google Custom Search) - https://serpapi.com/
# GOOGLE_CUSTOM_SEARCH_API_KEY= (gratuit, 100 requêtes/jour) - https://developers.google.com/custom-search/v1/overview
# GOOGLE_CUSTOM_SEARCH_ENGINE_ID= (ID du moteur de recherche personnalisé Google)

